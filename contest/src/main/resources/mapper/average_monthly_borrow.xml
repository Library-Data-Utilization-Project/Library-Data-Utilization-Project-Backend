<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.contest.rest.mapper.Average_monthly_borrowMapper">
	<!-- Create -->
	
	<!-- Read -->
	<select id="getAmbByMonth">
		SELECT *
		FROM average_monthly_borrow
		WHERE month = #{thisMonth}
	</select>
	<!-- Update -->

	<!-- delete -->
</mapper>